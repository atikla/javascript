/* global console , alert */ 

/* 
	Syntax : Math.min(val , val ,val ,val ,val);
	Syntax : Math.max(val , val ,val ,val ,val);

*/

var x = Math.min(100, 2, -15,1,4),
	y = Math.max(100, 2, -15,1,4);
console.log("the max mun is "+y+" and the min num is "+x );
